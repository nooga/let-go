
(ns tmp.referall (:require [core :refer :all] [test :refer :all]))

(deftest ns-require-refer-all
  (testing "ns require with :refer :all"
    (is (not (nil? (var +))) true)))

; (run-tests ns-require-refer-all)


