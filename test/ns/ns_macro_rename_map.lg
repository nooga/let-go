(ns tmp.rename 
  (:require [core :rename {+ plus - minus}])
  (:require [test :refer :all]))

(deftest ns-rename-map
  (testing "ns with :rename map"
    (is (= 3 (plus 1 2)))
    (is (= 1 (minus 3 2)))))


