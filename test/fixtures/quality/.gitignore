# The quality CLI test writes a scratch file here to prove that an UNTRACKED
# file is not measured (the corpus comes from the VCS file list). Ignoring it
# is what keeps it untracked -- jj snapshots the working copy on every
# command, so a merely-new file would be tracked by the time the tool asked.
scratch_*.lg
