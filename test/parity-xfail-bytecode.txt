# Shrink-only allowlist of Tier-1 fixtures that currently DIVERGE
# between bytecode and -tags gogen_ir engines.
# Managed by TestParityGatePhase1: re-seed with `LETGO_PARITY_REDERIVE=1 go test -run TestParityGatePhase1 ./test/e2e`.
# A new divergence fails CI; a listed fixture that now agrees also fails (shrink-only).
# When committing an allowlist update, include the specific issue/fix in the reason comment.

# PHASE-1 GATE: All Tier-1 fixtures currently agree. This allowlist remains empty.
