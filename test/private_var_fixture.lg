;; Fixture namespace for private_var_test.lg: a private and a public var
;; in a foreign namespace. Registers no tests — the TestRunner runs it as
;; a file, finds nothing, and moves on.

(ns private-var-fixture)

(defn- hidden [] 42)

(defn public-fn [] :pub)
