name: Go

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
      - name: Run golangci-lint
        run: golangci-lint run --timeout=5m

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Regenerate bundled bytecode
        run: go generate ./pkg/rt/

      # Content-based freshness gate. The previous step regenerated the bundle
      # from the committed .lg sources; if the committed bundle differs from
      # that regeneration it is stale (e.g. a merge/rebase that updated .lg but
      # not the bundle — the merge driver resolves the conflict but cannot emit
      # a fresh bundle mid-merge). Unlike the mtime-based bundle-fresh job, this
      # survives a fresh checkout. Fail loudly so a stale bundle never reaches
      # main, where `go install` would embed the wrong bytes.
      - name: Verify committed bundle matches regeneration
        run: |
          git diff --exit-code -- pkg/rt/core_compiled.lgb \
            || { echo "::error::pkg/rt/core_compiled.lgb is stale — run 'make generate' and commit it."; exit 1; }

      - name: Build
        run: go build -v ./...

      - name: Test with bundled bytecode stdlib
        run: go test -v -timeout 60s ./... -skip TestClojureTestSuite

      - name: Clojure compatibility test suite with bundled bytecode stdlib
        run: go test ./test/ -count=1 -run TestClojureTestSuite -v

      - name: Test with source stdlib bootstrap
        run: go test -tags bootstrap -v -timeout 60s ./... -skip TestClojureTestSuite

      - name: Clojure compatibility test suite with source stdlib bootstrap
        run: go test -tags bootstrap ./test/ -count=1 -run TestClojureTestSuite -v

  bundle-fresh:
    name: bundle-fresh
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Loud check — pkg/rt/core_compiled.lgb in lockstep with .lg sources
        # Policy: docs/contribution-policy.md §3 (stale bundle is a hard gate).
        # Fast (~1 s) — fails immediately when any pkg/rt/core/**/*.lg is
        # newer than the committed bundle.
        run: make check-bundle-fresh

      - name: Loud check — pkg/rt/core_go_lowered/ in lockstep with .lg sources
        # Policy: docs/contribution-policy.md §3 — sibling gate for the
        # -tags gogen_ir build path. parity-full's bucket hashes
        # silently diverge if the lgb is fresh but the lowered Go isn't.
        run: make check-lowered-fresh

  # Bootstrap-parity gate (deferred — runs as 'make parity-full' locally).
  #
  # Wiring 'make parity-full' as a CI job requires 'lgbgen --target=go' to
  # produce pkg/rt/core_go_lowered/ cleanly from current main. That gen
  # path errors today on a reader EOF — fixes land in a follow-up. Until
  # then, parity-full is a local-only target; the staleness gates
  # (check-bundle-fresh, check-lowered-fresh) protect the artifacts
  # parity-full depends on.
