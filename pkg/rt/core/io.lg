;; io — additional I/O utilities (see also native io namespace)
(ns io)

;; slurp-lines — convenience: slurp a file and split into vector of lines
(defn slurp-lines
  [path]
  (vec (read-lines path)))
